<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Cadastrado</title>
    <link rel="stylesheet" href="style.css">
</head>
<body>
<% 
    logado = false; 
    for (var i = 0; i < contas.length; i++) {
        if (login == contas[i][0] && senha == contas[i][1]) {
            logado = true;
            break;
        }
    }
%>

<% if (logado) { %>
    <div class="sucesso">
        <h1>Logado com sucesso!</h1>
    </div>
<% } else { %>
    <div class="incorreto">
        <h1>Login ou senha incorretos!</h1>
    </div> 
<% } %>

</body>
</html>